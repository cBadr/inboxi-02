; Inboxi inbound MTA — listen on port 25 for incoming mail.
listen=0.0.0.0:25

; Haraka 3.2.x reads cfg.haproxy.hosts at startup and crashes if the section is
; absent. Define an empty list (we don't sit behind HAProxy).
[haproxy]
hosts=
